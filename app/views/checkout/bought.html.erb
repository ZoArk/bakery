
<h2>Thank you for your order!</h2>

<p>Your order has been placed successfully. You will receive an email confirmation shortly.</p>

<p>Order Summary:</p>
<ul>
  <% @order.order_items.each do |item| %>
    <li><%= item.product.name %> - <%= item.quantity %> x <%= number_to_currency(item.product.price) %> = <%= number_to_currency(item.quantity * item.product.price) %></li>
  <% end %>
</ul>

<p>Total: <%= number_to_currency(@order.order_items.sum { |item| item.quantity * item.product.price }) %></p>

<%= link_to "Back to Shop", root_path, class: "btn btn-primary" %>
